# 剩余工作量与风险清单(排期用)

> 现状:架构/测试范式已验证(见 ARCHITECTURE.md),功能覆盖 ~2%(~27/1212 端点)。
> **M1 鉴权脊柱已完成**:本地 + OIDC(飞书/企业微信)登录、会话落 PG、令牌过期/登出、
> system-setting 全量 RBAC CRUD(用户/组织/角色/用户-角色),并抽出共享 `webauth` crate
> 把按资源 RBAC 铺到 project/case/bug/test-plan 的写端点(见 ARCHITECTURE §2.4)。
> 本文估算"补齐到可替代生产"的剩余工作,分**广度复制**与**硬骨头**两类。
>
> **估算口径(务必看):** 数字是**数量级**,非承诺。速度假设:在已验证模式下,
> 1 名熟练 Rust 工程师对"薄 CRUD(domain+ports+app+pg+http+测试)"约 **10–15 端点/周**,
> 对"逻辑/集成重"约 **3–6 端点/周**。端点数是粗代理(很多是 list/get 变体,少数极重)。
> 单位:**dev-week(人周)**。

---

## A. 广度复制(机械为主、可并行、低风险)

把已验证模式铺到剩余端点。已做 ~27 个(含 M1 鉴权 + system-setting RBAC CRUD),剩 ~1185。

| 域 | 端点(总/已做/剩) | 复杂度 | 速度假设 | 估时(人周) | 备注 |
|---|---|---|---|---|---|
| project-management | 195 / 2 / 193 | 中(环境/文件/全局参数 CRUD + 模块树) | 10/周 | **~19** | 文件管理、环境、模块树较重 |
| bug-management | 56 / 2 / 54 | 低-中 | 12/周 | **~5** | 评论/附件/关联用例/同步 |
| case-management | 142 / 1 / 141 | 中-高(评审流、自定义字段、脑图) | 6/周 | **~24** | 评审已验核心,其余 CRUD+导入导出 |
| test-plan | 156 / 2 / 154 | 中-高(编排、报告、定时) | 6/周 | **~26** | 统计已验核心,执行编排重 |
| system-setting | 356 / ~20 / ~336 | 中(海量配置 CRUD)+ 鉴权(见 B1) | 12/周 | **~28** | 端点最多;鉴权 + 用户/组织/角色 RBAC CRUD 已做 |
| api-test | 269 / 1 / 268 | 高(定义/场景/Mock/导入)+ 引擎(见 B2) | 5/周 | **~54** | 引擎单列到 B;此处仅 CRUD/解析 |
| dashboard | 38 / 0 / 38 | 中(聚合查询) | 8/周 | **~5** | 依赖各域数据就绪 |
| **小计** | **~1185 剩** | | | **~158 人周** | ≈ 3 人 × 1 年(纯广度) |

> 广度可并行:多人按域分工,墙钟时间可压到数月。这部分**几乎无架构风险**,是体力+评审。

---

## B. 硬骨头(有实质风险/需设计,单列)

| # | 项 | 为何难 / 风险 | 估时 | 风险 |
|---|---|---|---|---|
| B1 | **鉴权/会话/RBAC 落地** | 🟢 **M1 已完成并真库验证**:本地登录(Argon2 + 防账号枚举)、`AuthUser` 提取器(Bearer→401)、`kernel` 按资源 RBAC(不足→403);凭证/会话落 PG(`ms_user_credential`/`ms_session`,`text[]` 存权限),**会话跨 server 重启存活**,令牌**过期 + 登出**(撤销),启动幂等 upsert admin;**OIDC 登录(飞书 + 企业微信)** 经 OAuth2 抽象 + CFT 旁路 quirk;system-setting **全量 RBAC CRUD**(用户/组织/角色/用户-角色授权);抽出共享 **`webauth` crate**,把按资源 RBAC **铺到 project/case/bug/test-plan 写端点**(见 §2.4)。**剩余**:OIDC state CSRF 校验、令牌刷新、LDAP、把校验铺到后续新增写端点 | 剩 ~1–3 周 | 高(安全正确性) |
| B2 | **JMeter 执行引擎内部** | 跑 .jmx、分布式压测、结果回收。**建议不重写**:保留 Java 执行器为独立服务,藏在已有 `TaskDispatcher` 端口后(下发已通)。原生重写=数月且高风险 | 包壳 ~3–6 周 / 重写 6+ 月 | 极高(若重写) |
| B3 | **执行/消息基础设施** | Kafka(rdkafka)、K8s 调度(kube-rs)、MinIO 对象存储——各自 FFI/运维耦合 | 各 ~2–4 周 | 中-高 |
| B4 | **插件系统** | Java 用 classloader 动态加载 jar;Rust 动态加载困难,需重新设计(WASM?进程隔离?) | ~4–8 周 + 设计 | 高(架构未定) |
| B5 | **报告 / 通知 / 定时任务 / i18n** | 报告聚合与分享、消息机器人、cron 调度、多语言 | 各 ~2–4 周 | 中 |
| B6 | **数据迁移工程化** | pgloader 脚本现仅模板(未对真实 MySQL schema 跑通);影子双跑校验工具未做;一次性 cutover 演练 | ~3–5 周 | 中-高(数据完整性) |
| | **小计(不含 B2 重写)** | | **~25–45 人周** | |

---

## C. 生产化收尾(已部分完成)

- ✅ 版本化迁移、健康探针、结构化日志、优雅关闭、超时/体积限制
- ☐ 可观测性:metrics(Prometheus)+ 分布式追踪(OpenTelemetry) ~2–3 周
- ☐ 配置体系(分环境)、CI/CD、容器/Helm(复用现有 helm/) ~2–4 周
- ☐ 限流、CORS、统一错误响应体(problem+json) ~1–2 周

---

## D. 量级总结

| 口径 | 估算 |
|---|---|
| 纯广度复制(A) | ~160 人周 |
| 硬骨头(B,不含 JMeter 重写) | ~25–45 人周 |
| 生产化收尾(C) | ~5–9 人周 |
| **合计(JMeter 采用"包壳"策略)** | **~190–215 人周 ≈ 4 名工程师 × ~1 年** |
| 若坚持原生重写 JMeter 引擎 | 再 **+6 个月以上**,且高风险 |

---

## E. 给排期的关键建议

1. **不要原生重写 JMeter 引擎**(B2):保留 Java 执行器为服务,藏在 `TaskDispatcher` 后。功能用例可用已验证的原生 `api-runner`,压测走 Java/Goose。这一条决定能砍掉数月与最大风险。
2. **先 B1 鉴权**:它是所有端点的横切前置,缺它则广度成果不可上线。优先级最高。
3. **广度分域并行**(A):模式已固定,多人按域推进;每域先补"读"端点(低风险)再补"写"。
4. **B6 迁移工程化要早做、早演练**:数据完整性风险只能靠真实 schema 上反复 dry-run + 影子双跑暴露。
5. **descope 候选**:dashboard、插件系统(B4)若非必需,首期可不做——能再省可观的工作量与一个架构难题。
6. **里程碑切分**:M1=鉴权+system-setting/project 全量(可登录可管项目);M2=case/bug/test-plan 全量;M3=api-test CRUD+执行包壳;M4=dashboard/插件/可观测性收尾。
