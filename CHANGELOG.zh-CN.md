# 变更日志

格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);版本号遵循 SemVer。
未发布的改动放在 Unreleased 下,发布时移入对应的版本小节。

> English: [CHANGELOG.md](CHANGELOG.md).

## [Unreleased]

## [0.0.3] - 2026-07-18

### 新增
- 功能用例详情抽屉(详情/变更历史/缺陷/评审/测试计划/依赖关系等页签,抽屉内就地编辑,关注与评论)
- 场景批量能力:数字编号、可排序列、底部批量条、批量执行弹窗(集合报告、资源池准入与并发约束)
- 场景回收站(软删保留步骤,恢复/彻底删除/批量操作)
- 导入系统请求区分复制与引用:复制为可完整编辑的内联请求并带来源标签;场景复制挂载为独立副本组;引用数据刷新按钮
- 每次请求采集 HTTP 分段耗时(DNS/TTFB/下载),Chrome 风格耗时组件在报告/步骤结果/调试台共享
- 运行时变量 `${__runid}` / `${__timestamp}` 让持久资源链可重复执行;全模块自举回归可连续重跑全绿
- 测试计划:支持挂载场景;计划执行统一走场景执行器,行内可查看场景报告;计划详情页(测试规划脑图:测试点/配置叶子/关联用例弹窗;场景用例页签:测试点/模块树、单条执行/取消关联;头部与页签);新建/编辑右侧抽屉;定时任务删除端点
- 用例评审元数据(名称/描述/标签/所属模块/评审周期)与重构后的评审列表页(模块树+编辑抽屉)
- 待办/关注工作台改版(分模块卡片;关注页六个模块)
- 登录页重设计(双面板滑动+logo 动画);技能指令支持 Markdown 编辑器
- openapi-bootstrap 技能英文化,新增测试计划操作自检脚本

### 变更
- 整体扁平化:阴影与遮罩减淡,右侧抽屉全面支持拖拽调宽
- README 截图换成单图昼夜对角线合成

### 修复
- 计划挂载场景不再因缺少运行时变量而失败(计划执行委托场景执行器)
- 重存计划定时任务替换内存 cron 任务而非叠加;删除计划后不再有幽灵任务空转
- 开发代理覆盖 `/follow` 与 `/comment`;README 状态徽章锚点

## [0.0.2] - 2026-07-14

### 新增
- 缺陷 ↔ 需求 / 场景用例 / 功能用例 的关联(可追溯链路),包含关联抽屉 UI
- 派发可指定某个已注册的运行时(按名称;在 Redis 下使用独立 stream,离线的目标任务保持排队)
- 项目成员 / 用户组管理接入后端(添加成员、创建组、移除)
- 每个运行时独立的 API Key(`sak_` 前缀,argon2 存储,60 秒校验缓存,可吊销),含 Web 管理页面与 agent-runtime 的 `SHEPHERD_AGENT_KEY` 静态凭据模式
- 登录失败锁定:同一用户名连续失败 5 次锁定 15 分钟(HTTP 429)
- SECURITY.md / CONTRIBUTING.md / LICENSE(GPL-2.0)/ CHANGELOG

### 变更
- 所有读取端点现在都需要认证 + `READ` 权限(此前约 50 个 GET 端点可匿名访问)
- 限流默认开启(每客户端 200 rps;`SHEPHERD_RATE_LIMIT_RPS=0` 可关闭)
- 检测到弱默认管理员密码时,server 与 agent-runtime 会在启动时告警
- Web 配色改为清爽的浅蓝(Arco 风格);默认深色主题为中性深灰
- 所有 workspace 成员 `publish = false`(不把共享 crate 名发布到 crates.io)

### 修复
- Mutex 中毒现在会恢复,而不再级联 panic(请求路径上的限流器 / 指标 / 会话)
- 英文语言下的左侧导航溢出;若干 clippy 修复,包括测试超时路径未回收子进程的问题
